/*
 * SissyTrends Boutique — Environment Config TEMPLATE
 * Safe to commit — contains NO real credentials.
 *
 * SETUP:
 * 1. Copy this file → admin/env-config.js
 * 2. Fill in your real username and password
 * 3. env-config.js is in .gitignore — it will never be committed
 */
window.__ENV = {
  ADMIN_USERNAME: '',   // e.g. 'sissytrends_admin'
  ADMIN_PASSWORD: '',   // e.g. 'YourSecurePassword@2025'
};
